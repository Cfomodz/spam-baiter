# Optional: enables the TTS panel. Without it, TTS endpoints return 503.
ELEVENLABS_API_KEY=your_key_here

# "mock" is the only working bridge today; "bluetooth" is reserved and will
# fail at startup with a clear error until a real bridge is implemented.
PHONE_BRIDGE=mock
